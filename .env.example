# Example environment variables (no real secrets)

# Optional: GitHub personal access token for private repos / rate limits
# GITHUB_TOKEN=

# Python doc-engine base URL (default http://127.0.0.1:8765)
# DOC_ENGINE_URL=http://127.0.0.1:8765
